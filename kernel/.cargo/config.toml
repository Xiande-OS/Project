# Lets `cargo build` (without --target) inside kernel/ Just Work.
# `cargo xtask` from the workspace root passes --target explicitly
# and picks up the linker flags from ../.cargo/config.toml.
[build]
target = "riscv64gc-unknown-none-elf"
