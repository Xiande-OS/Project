[workspace]
resolver = "2"
members = ["kernel", "xtask"]

[workspace.package]
edition = "2021"
license = "MIT OR Apache-2.0"
authors = ["xiande-os contributors"]

[profile.release]
debug = true
lto = false
codegen-units = 1
panic = "abort"

[profile.dev]
panic = "abort"
